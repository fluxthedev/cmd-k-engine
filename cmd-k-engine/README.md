App
 │
 │ registers commands
 ▼
useCommandPalette()
 │
 ├── stores commands
 ├── searches commands
 ├── tracks selected command
 ├── handles submenus
 ├── handles keyboard
 └── opens/closes palette
 │
 ▼
CommandPalette.vue
    displays the state
